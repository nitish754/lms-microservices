{
  "family": "course-service",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "arn:aws:iam::<ACCOUNT_ID>:role/ecsTaskExecutionRole",
  "taskRoleArn": "arn:aws:iam::<ACCOUNT_ID>:role/course-service-task-role",
  "containerDefinitions": [
    {
      "name": "course-service",
      "image": "<ACCOUNT_ID>.dkr.ecr.<REGION>.amazonaws.com/course-service:<TAG>",
      "essential": true,
      "portMappings": [
        {
          "containerPort": 4003,
          "protocol": "tcp"
        }
      ],
      "environment": [
        { "name": "SERVICE_NAME", "value": "course-service" },
        { "name": "PORT", "value": "4003" }
      ],
      "secrets": [ { "name": "DATABASE_URL", "valueFrom": "arn:aws:secretsmanager:<REGION>:<ACCOUNT_ID>:secret:course-service/DATABASE_URL" } ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/course-service",
          "awslogs-region": "<REGION>",
          "awslogs-stream-prefix": "ecs"
        }
      }
    }
  ]
}
